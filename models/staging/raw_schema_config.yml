version: 2
 
sources:
  - name: raw_qwt
    database:  "{{env_var('DBT_SOURCEDB', 'DBT_DB')}}"
    schema: "{{env_var('DBT_RAWSCHEMA', 'RAW_DEV')}}"
    tables:
      - name: RAW_CUSTOMER
      - name: RAW_PRODUCTS
      - name: raw_orders
      - name: raw_order_detail
      - name: raw_employee
      - name: raw_offices
      - name: raw_shipments
      - name: raw_suppliers_xml